lenguaje: ruby

before_install:
  - gem install sinatra
  - gem install rspec
  - gem install cucumber
  - gem install rack-test
  - gem install webrat
  - gem install rspec
  - gem install simplecov
  - gem install simplecov-console

rvm:
  - 2.2
  
script:
  - cucumber
  - rspec